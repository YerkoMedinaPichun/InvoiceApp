import React from "react";
import ReactDOM from "react-dom/client";
import { InvocieApp } from "./InvoiceApp";

ReactDOM.createRoot(document.getElementById("root")).render(
	<React.StrictMode>
		<InvocieApp />
	</React.StrictMode>
);
